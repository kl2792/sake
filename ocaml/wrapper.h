#ifndef __WRAPPER_H__ // Where name is the name of the sk file
#define __WRAPPER_H__

struct wrapper_input {
};

struct wrapper_output {
};

struct wrapper_state {
};

struct wrapper_tick {
};

void wrapper_tick(struct name_state *, struct name_input *, struct name_output *);

#endif 
